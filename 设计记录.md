# 设计记录

## 1. 项目目标与范围
构建一个在 Windows 下单机运行的 C/S 应用，用于连接已有 MySQL 数据库，读取单位、人员、体检结果及异常值，在前端展示并支持 Excel 导出（异常值红色标记）。

## 2. 讨论纪要（按日期）
### 2026-03-05
1. 项目为新建项目，先搭建通用框架。
2. 项目目录命名建议避免空格，推荐 `boda-export-data`。
3. 核心功能确认：连接数据库、读取并展示数据、导出 Excel 并标记异常。
4. 技术约束确认：数据库 MySQL、异常字段已有、运行在 Windows。
5. 项目根目录新增 `设计记录.md`。
6. 已同意先实现通用框架，真实表结构后补 MySQL 映射。

## 3. 架构设计
### 3.1 总体架构
1. 桌面端：Electron（应用壳 + 一键启动）。
2. 前端：React + Ant Design。
3. 后端：FastAPI。
4. 数据库：MySQL。
5. 导出：openpyxl。

### 3.2 数据流
1. 前端发起筛选查询请求到本地后端 API。
2. 后端访问 MySQL 查询并统一返回结构。
3. 前端渲染并高亮异常值。
4. 前端触发导出，后端生成 Excel。

## 4. 数据模型与接口约定
### 4.1 通用模型
1. Org: `org_id`, `org_name`
2. Person: `person_id`, `org_id`, `name`, `gender`, `id_no`
3. ExamRecord: `record_id`, `person_id`, `exam_date`, `item_code`, `item_name`, `result_value`, `unit`, `ref_range`, `is_abnormal`, `abnormal_flag`

### 4.2 API
1. `GET /api/health`
2. `GET /api/orgs`
3. `GET /api/records`
4. `POST /api/export`

## 5. 前端设计与交互步骤
1. 筛选区：单位、姓名/证件号、日期范围、仅异常。
2. 数据表：分页、排序、异常红色显示。
3. 导出：按当前筛选条件导出。

## 6. 后端实现步骤
1. 配置加载（app/db/export）。
2. 数据访问层（当前 mock，后续切 MySQL）。
3. 导出服务（openpyxl + 异常红色）。
4. 统一异常处理与日志。

## 7. 导出 Excel 设计
1. 列顺序：`config/export_columns.yaml`
2. 文件名：`体检导出_yyyyMMdd_HHmmss.xlsx`
3. 样式：异常值红色字体。

## 8. 部署与打包步骤（Windows）
1. 开发阶段前后端分离启动。
2. 生产阶段由 Electron 拉起本地 FastAPI。
3. 使用 electron-builder 打包 Setup.exe。

## 9. 配置项说明
1. `config/app.yaml`
2. `config/db.yaml`
3. `config/export_columns.yaml`

## 10. 风险与待确认事项
1. 待提供真实表结构。
2. 导出模板细节待确认。
3. 大数据量导出性能待评估。

## 11. 里程碑
1. M1：项目骨架与占位接口。
2. M2：MySQL 真表映射。
3. M3：前端联调。
4. M4：导出模板定稿。
5. M5：Windows 打包验证。

## 12. 当前实现状态（2026-03-05）
1. 已完成项目目录骨架：`app-api`、`app-web`、`app-desktop`、`config`、`scripts`。
2. 已完成后端接口：`/api/health`、`/api/orgs`、`/api/records`、`/api/export`。
3. 已实现 Excel 导出，异常值字段采用红色字体标记。
4. 当前数据访问层为 mock 数据，等待真实 MySQL 表结构后替换。

## 13. 验证记录（2026-03-05）
1. `GET /api/health` 返回 200。
2. `GET /api/orgs` 返回单位列表。
3. `GET /api/records?only_abnormal=true` 返回异常记录。
4. `POST /api/export` 成功生成 Excel 文件。

## 14. 下一步计划
1. 根据真实表结构实现 MySQL 查询映射。
2. 实现 React 页面并联调后端接口。
3. 实现 Electron 启动本地后端并完成一键打包。

---
最后更新：2026-03-07

## 15. 前端阶段进展（2026-03-05）
1. 已完成 React + Ant Design 首版页面框架。
2. 已完成筛选区（单位、关键字、日期区间、仅异常）。
3. 已完成数据表格展示与异常值红色标记。
4. 已完成导出按钮对接 `/api/export`。
## 16. 列表结构调整（2026-03-05）
1. 主表粒度调整为“人员 + 体检编号”（一行一单）。
2. 查询条件新增：汇总日期、终检日期、体检状态、体检编号。
3. 主表字段新增：汇总日期、终检日期、体检状态、电话、体检编号。
4. 详情展示采用“一次展开”：展开后直接展示项目组及其参数明细。
5. 项目组行显示组异常状态，参数行显示结果值/单位/参考范围/异常标记。
## 17. 用户管理与导出增强（2026-03-05）
1. 新增登录能力：用户名/密码登录，后端签发 JWT。
2. 新增认证接口：`/api/auth/login`、`/api/auth/me`。
3. 数据接口加登录保护：未登录访问返回 401。
4. 导出能力增强：支持指定导出目录 `export_dir`。
5. 导出能力增强：支持选择组合项目 `selected_groups`，选中后该组下细项一并导出。
6. 前端新增导出设置弹窗，填写导出目录并多选组合项目。
## 18. 用户管理首版（2026-03-05）
1. 新增管理员接口：用户列表、新增用户、启用/禁用、重置密码。
2. 权限规则：仅 `admin` 可访问用户管理接口，`viewer` 返回 403。
3. 前端新增“用户管理”弹窗（仅管理员可见）。
4. 用户管理支持：创建用户、切换启用状态、重置密码为 `123456`。
## 19. 导出样式改造（2026-03-05）
1. 导出改为宽表：一条体检记录（人员+体检编号）占一行。
2. 细项名称作为横向动态表头展开。
3. 每个细项前增加“组合结果”列，内容为“组合项(正常/异常)”，列底色浅蓝。
4. 异常细项结果值使用红色字体标记。
## 20. 视图接入准备（2026-03-05）
1. 新增双模式数据层：`db.yaml enabled=true` 时读 MySQL 视图，否则回退 mock。
2. 新增 `view_mapping.yaml`：配置标准字段到视图字段映射。
3. 新增 `dict_mapping.yaml`：配置状态/性别/异常值字典映射。
4. 当前已根据样例字段预置映射：
   - `ENTERPRICE -> org_name`
   - `SGLCHECKID -> exam_no`
   - `NAME -> person_name`
   - `SEX -> gender`
   - `MOBILEPHONE -> phone`
   - `ANALYZEDATE -> summary_date`
   - `AUDITEDATE -> final_date`
   - `CHECKSTATUS -> exam_status`
   - `CHECKMODULENAME -> group_name`
   - `CHECKITEMNAME -> item_name`
   - `ITEMRESULT -> result_value`
   - `UNIT -> unit`
   - `CRITICALNAME -> abnormal_flag`
## 21. 分页优化（2026-03-05）
1. 查询分页改为按体检单（exam_no）分页，避免明细行被分页拆断。
2. 后端流程：先分页取 exam_no，再回查该页完整项目明细。
3. 前端每页条数固定可选：20 / 50 / 100。
4. 后端 page_size 上限限制为 100。
## 22. SQLServer 真实接入与安全说明（2026-03-05）
1. 已支持 SQLServer 视图直连（`db_type: sqlserver`）。
2. 已按 `view_mapping.yaml` 与 `dict_mapping.yaml` 做字段与字典转换。
3. 查询分页改为按体检单（exam_no）分页，再回查该页全部明细，避免分页拆断。
4. 前端单位下拉支持模糊搜索（输入关键字过滤）。
5. 数据库访问策略为只读查询（SELECT），不执行写入语句。
## 23. 项目启动步骤（Windows）
1. 打开两个 PowerShell 窗口，进入项目目录：
```powershell
cd "D:\project\boda-export-data"
```
2. 启动后端（窗口1）：
```powershell
.\.venv\Scripts\Activate.ps1
python .\app-api\run.py
```
3. 启动前端（窗口2）：
```powershell
cd "D:\project\boda-export-data\app-web"
npm run dev
```
4. 访问地址：
   - 前端：`http://127.0.0.1:5173`
   - 后端：`http://127.0.0.1:8000`
   - 健康检查：`http://127.0.0.1:8000/api/health`
5. 登录测试账号：
   - `admin / admin123`
   - `viewer / viewer123`
6. 首次启动或依赖变更时，先安装依赖：
```powershell
cd "D:\project\boda-export-data"
.\.venv\Scripts\Activate.ps1
pip install -r .\app-api\requirements.txt

cd "D:\project\boda-export-data\app-web"
npm install
```
## 24. 查询性能优化（2026-03-06）
1. 默认查询范围改为最近30天（未传日期条件时自动应用）。
2. 前端首次加载与重置时默认带最近30天日期区间。
3. 单位列表与项目组列表增加 TTL 缓存，减少重复 DISTINCT 查询。
4. 总数统计（COUNT DISTINCT）增加按条件缓存，缓存命中时不重复计算。
5. 配置项新增：
   - `default_query_days`
   - `cache_ttl_orgs_seconds`
   - `cache_ttl_groups_seconds`
   - `cache_ttl_total_seconds`

## 25. 桌面安装包一体化（2026-03-06）
1. 已将后端服务打包为独立可执行文件 `boda-api.exe` 并随安装包分发。
2. Electron 启动时先探活 `http://127.0.0.1:8000/api/health`，若未启动则自动拉起本地后端。
3. Electron 退出时会尝试关闭由应用拉起的后端进程。
4. 安装包已包含：
   - 前端静态资源（`app-web-dist`）
   - 后端可执行文件（`backend-api/boda-api.exe`）
   - 配置目录模板（`config/*.yaml`）
5. 前端导出目录支持“选择目录”按钮（桌面模式走系统目录选择对话框）。

## 26. 生产环境配置与上线说明（2026-03-06）
1. 安装后实际运行配置目录：
   - `C:\Users\<用户名>\AppData\Roaming\Boda Export Data\runtime\config\`
2. 数据库连接以运行目录下 `db.yaml` 为准；如需接生产库，修改该文件并重启应用。
3. 首次启动会将安装包内 `config` 模板复制到上述运行目录；后续不会覆盖已存在文件。
4. 若部署到生产环境，需确保：
   - 生产机可访问数据库主机与端口。
   - 数据库账号具备只读查询权限。
   - `view_mapping.yaml` 与 `dict_mapping.yaml` 与生产字段保持一致。
5. 一键打包脚本：`scripts/build-win.ps1`
   - 步骤：构建后端 exe -> 构建前端 -> 构建桌面安装包。
## 27. 本轮修订记录（2026-03-07）
1. 登录页已移除默认测试账号/密码提示文案。
2. 已重新生成 Windows 安装包：
   - `app-desktop/dist/Boda Export Data Setup 0.1.0.exe`
3. 安装包仍包含后端一体化能力（`backend-api/boda-api.exe`），支持桌面启动自动拉起后端。
4. 本次变更基于分支：`fix/prod-db-error-visible`。
## 28. 本轮修复记录（2026-03-07）
1. 生产环境 SQLServer 连接链路修复：后端在 `sqlserver` 模式下先尝试 `pyodbc`，失败后自动回退 `pymssql`，并输出可诊断日志。
2. 安装包构建链路修复：`requirements.txt` 与 `build-win.ps1` 已纳入 `pyodbc` 打包，避免 `No module named pyodbc`。
3. 桌面端导出能力增强：导出弹窗增加“选择目录”按钮，调用 Electron 目录选择对话框回填 `export_dir`。
4. 前端乱码修复：`app-web/src/App.jsx` 恢复为正常中文文案并统一 UTF-8，修复“确认/导出”等中文乱码显示问题。
5. 重新构建安装包并验证：`app-desktop/dist/Boda Export Data Setup 0.1.0.exe`（2026-03-07 11:58:53）。

## 29. 本轮修复记录（2026-03-11）
1. 视图字段映射新增身份证字段：`config/view_mapping.yaml` 增加 `id_no: IDCARD`。
2. 后端记录组装修复：`app-api/app/services/data_service.py` 在数据库结果转 `ExamSummaryRecord` 时补回 `id_no`，并同步保留 `exam_no`、`summary_date`。
3. 已启动本地前后端验证接口可用，并重新构建生产安装包。
4. 最新安装包：`app-desktop/dist/Boda Export Data Setup 0.1.0.exe`，时间为 2026-03-11 16:25:32。
