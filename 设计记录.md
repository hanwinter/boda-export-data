# 设计记录

## 1. 项目目标与范围
构建一个在 Windows 下单机运行的 C/S 应用，用于连接已有 MySQL 数据库，读取单位、人员、体检结果及异常值，在前端展示并支持 Excel 导出（异常值红色标记）。

## 2. 讨论纪要（按日期）
### 2026-03-05
1. 项目为新建项目，先搭建通用框架。
2. 项目目录命名建议避免空格，推荐 `boda-export-data`。
3. 核心功能确认：连接数据库、读取并展示数据、导出 Excel 并标记异常。
4. 技术约束确认：数据库 MySQL、异常字段已有、运行在 Windows。
5. 项目根目录新增 `设计记录.md`。
6. 已同意先实现通用框架，真实表结构后补 MySQL 映射。

## 3. 架构设计
### 3.1 总体架构
1. 桌面端：Electron（应用壳 + 一键启动）。
2. 前端：React + Ant Design。
3. 后端：FastAPI。
4. 数据库：MySQL。
5. 导出：openpyxl。

### 3.2 数据流
1. 前端发起筛选查询请求到本地后端 API。
2. 后端访问 MySQL 查询并统一返回结构。
3. 前端渲染并高亮异常值。
4. 前端触发导出，后端生成 Excel。

## 4. 数据模型与接口约定
### 4.1 通用模型
1. Org: `org_id`, `org_name`
2. Person: `person_id`, `org_id`, `name`, `gender`, `id_no`
3. ExamRecord: `record_id`, `person_id`, `exam_date`, `item_code`, `item_name`, `result_value`, `unit`, `ref_range`, `is_abnormal`, `abnormal_flag`

### 4.2 API
1. `GET /api/health`
2. `GET /api/orgs`
3. `GET /api/records`
4. `POST /api/export`

## 5. 前端设计与交互步骤
1. 筛选区：单位、姓名/证件号、日期范围、仅异常。
2. 数据表：分页、排序、异常红色显示。
3. 导出：按当前筛选条件导出。

## 6. 后端实现步骤
1. 配置加载（app/db/export）。
2. 数据访问层（当前 mock，后续切 MySQL）。
3. 导出服务（openpyxl + 异常红色）。
4. 统一异常处理与日志。

## 7. 导出 Excel 设计
1. 列顺序：`config/export_columns.yaml`
2. 文件名：`体检导出_yyyyMMdd_HHmmss.xlsx`
3. 样式：异常值红色字体。

## 8. 部署与打包步骤（Windows）
1. 开发阶段前后端分离启动。
2. 生产阶段由 Electron 拉起本地 FastAPI。
3. 使用 electron-builder 打包 Setup.exe。

## 9. 配置项说明
1. `config/app.yaml`
2. `config/db.yaml`
3. `config/export_columns.yaml`

## 10. 风险与待确认事项
1. 待提供真实表结构。
2. 导出模板细节待确认。
3. 大数据量导出性能待评估。

## 11. 里程碑
1. M1：项目骨架与占位接口。
2. M2：MySQL 真表映射。
3. M3：前端联调。
4. M4：导出模板定稿。
5. M5：Windows 打包验证。

## 12. 当前实现状态（2026-03-05）
1. 已完成项目目录骨架：`app-api`、`app-web`、`app-desktop`、`config`、`scripts`。
2. 已完成后端接口：`/api/health`、`/api/orgs`、`/api/records`、`/api/export`。
3. 已实现 Excel 导出，异常值字段采用红色字体标记。
4. 当前数据访问层为 mock 数据，等待真实 MySQL 表结构后替换。

## 13. 验证记录（2026-03-05）
1. `GET /api/health` 返回 200。
2. `GET /api/orgs` 返回单位列表。
3. `GET /api/records?only_abnormal=true` 返回异常记录。
4. `POST /api/export` 成功生成 Excel 文件。

## 14. 下一步计划
1. 根据真实表结构实现 MySQL 查询映射。
2. 实现 React 页面并联调后端接口。
3. 实现 Electron 启动本地后端并完成一键打包。

---
最后更新：2026-03-05
